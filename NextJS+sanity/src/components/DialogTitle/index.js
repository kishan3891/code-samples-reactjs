export { DialogTitle as default } from '@material-ui/core'

export const styles = () => ({
  root: {
    textAlign: 'right', // we are only using DialogTitle to provide a close button which should be right aligned
  },
})
